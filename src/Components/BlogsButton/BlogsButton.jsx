import { Button } from "./BlogsButton.styles";

export default function BlogsButton() {
    return (
        <Button>
            <a href="/blogs">
                <span>Blogs</span>
            </a>
        </Button>
    );
}